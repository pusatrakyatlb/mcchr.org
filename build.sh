### Cloudflare Pages
### Don't forget to set the `Build command` field value to `bash build.sh` in the project settings

hugo # Hugo Jamstack framework

npm i stripe # Stripe Checkout via /functions/api/pay.js